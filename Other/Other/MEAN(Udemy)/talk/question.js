var answer = "Now that's a good question!";

module.exports.ask = function(question){ // defined a mathod called 'ask' that accepts a parameter called question
    console.log(question);
    return answer;
};
